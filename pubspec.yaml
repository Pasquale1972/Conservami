name: conservami_app
description: App per gestire alimenti in frigorifero con Hive e scansione barcode
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  flutter_barcode_scanner: ^2.0.0
  http: ^1.4.0
  flutter_local_notifications: ^19.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  hive_generator:
